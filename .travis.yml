language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-jmpress' ]; then cd .. && eval "mv $currentfolder generator-jmpress" && cd generator-jmpress; fi

